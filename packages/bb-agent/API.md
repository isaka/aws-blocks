## API Report File for "@aws-blocks/bb-agent"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import type { ChildLogger } from '@aws-blocks/bb-logger';
import { FileBucket } from '@aws-blocks/bb-file-bucket';
import { RealtimeChannel } from '@aws-blocks/bb-realtime';
import { Scope } from '@aws-blocks/core';
import type { ScopeParent } from '@aws-blocks/core';
import type { SnapshotStorage } from '@strands-agents/sdk';
import type { z } from 'zod';

// Warning: (ae-forgotten-export) The symbol "AgentBase" needs to be exported by the entry point index.aws.d.ts
//
// @public (undocumented)
export class Agent<TContext = DefaultToolContext> extends AgentBase<TContext> {
    constructor(scope: ScopeParent, id: string, config: AgentConfig<TContext>);
}

// @public (undocumented)
export interface AgentConfig<TContext = DefaultToolContext> {
    // Warning: (ae-forgotten-export) The symbol "ConversationManagerConfig" needs to be exported by the entry point index.aws.d.ts
    //
    // (undocumented)
    conversation?: ConversationManagerConfig;
    // (undocumented)
    description?: string;
    inferenceOnly?: boolean;
    logger?: ChildLogger;
    // (undocumented)
    model?: {
        deployed?: ModelConfig | ModelConfig[];
        local?: ModelConfig | ModelConfig[];
    };
    // (undocumented)
    name?: string;
    removalPolicy?: 'destroy' | 'retain';
    streamingMode?: 'token' | 'block';
    // (undocumented)
    structuredOutput?: z.ZodType;
    // (undocumented)
    systemPrompt: string;
    toolContextSchema?: z.ZodType<TContext>;
    tools?: ToolsConfig<TContext>;
}

// @public
export const AgentErrors: {
    readonly PersistenceRequired: "PersistenceRequiredException";
    readonly InvalidModelConfig: "InvalidModelConfigException";
    readonly ModelUnavailable: "ModelUnavailableException";
    readonly BrowserNotSupported: "BrowserNotSupportedException";
    readonly StreamFailed: "StreamFailedException";
    readonly InterruptRequired: "InterruptRequiredException";
};

// @public (undocumented)
export interface AgentResult {
    // (undocumented)
    text: string;
    // (undocumented)
    toolCalls: ToolCallRecord[];
    // (undocumented)
    usage?: TokenUsage;
}

// @public (undocumented)
export interface AgentStreamChunk {
    // (undocumented)
    error?: string;
    // (undocumented)
    input?: JSONValue;
    // (undocumented)
    interrupts?: Array<{
        id: string;
        name: string;
        reason?: any;
    }>;
    // (undocumented)
    text?: string;
    // (undocumented)
    toolName?: string;
    // (undocumented)
    type: 'text-delta' | 'tool-call' | 'tool-result' | 'done' | 'error' | 'interrupt';
    // (undocumented)
    usage?: TokenUsage;
}

// @public
export interface AgentStreamResult {
    channel: Promise<RealtimeChannel<AgentStreamChunk>>;
    channelId: string;
    complete: () => Promise<AgentStreamChunk>;
    toJSON(): {
        channelId: string;
        channel: null;
    };
}

// @public
export type AgentTool<TContext = DefaultToolContext> = ToolDefinition<TContext, any> & {
    readonly [AGENT_TOOL_BRAND]: true;
};

// @public
export const BedrockModels: {
    readonly BALANCED: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-sonnet-4-6";
    };
    readonly SMART: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-opus-4-8";
    };
    readonly FAST: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-haiku-4-5-20251001-v1:0";
    };
    readonly DEFAULT: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-sonnet-4-6";
    };
    readonly BUDGET: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-haiku-4-5-20251001-v1:0";
    };
    readonly MICRO: {
        readonly provider: "bedrock";
        readonly modelId: "global.anthropic.claude-haiku-4-5-20251001-v1:0";
    };
};

// @public (undocumented)
export interface Conversation {
    // (undocumented)
    conversationId: string;
    // (undocumented)
    createdAt: number;
    // (undocumented)
    name: string;
    // (undocumented)
    updatedAt: number;
}

// @public
export type DefaultToolContext = Record<string, any>;

// @public (undocumented)
export class InterruptError extends Error {
    constructor(message: string, interrupts: Array<{
        id: string;
        name: string;
        reason?: any;
    }>);
    // (undocumented)
    readonly interrupts: Array<{
        id: string;
        name: string;
        reason?: any;
    }>;
}

// @public
export interface InterruptResponse {
    approved?: boolean;
    input?: any;
    interruptId: string;
    response?: JSONValue;
    toolName?: string;
    trust?: boolean;
}

// @public
export type JSONValue = string | number | boolean | null | {
    [key: string]: JSONValue;
} | JSONValue[];

// @public (undocumented)
export interface Message {
    // (undocumented)
    content: string;
    // (undocumented)
    contentType: 'text' | 'image' | 'audio' | 'video' | 'document';
    // (undocumented)
    createdAt: number;
    // (undocumented)
    messageId: string;
    // Warning: (ae-forgotten-export) The symbol "MessageMetadata" needs to be exported by the entry point index.aws.d.ts
    //
    // (undocumented)
    metadata: MessageMetadata;
    // (undocumented)
    role: 'user' | 'assistant' | 'tool-call' | 'tool-result' | 'approval' | 'interrupt';
}

// @public (undocumented)
export interface ModelConfig {
    apiKey?: string | (() => Promise<string>);
    // (undocumented)
    endpoint?: string;
    // Warning: (ae-forgotten-export) The symbol "GuardrailsConfig" needs to be exported by the entry point index.aws.d.ts
    //
    // (undocumented)
    guardrails?: GuardrailsConfig;
    // Warning: (ae-forgotten-export) The symbol "InferenceConfig" needs to be exported by the entry point index.aws.d.ts
    //
    // (undocumented)
    inferenceConfig?: InferenceConfig;
    // (undocumented)
    modelId?: string;
    provider: 'bedrock' | 'openai-api' | 'canned';
}

// @public
export const OllamaModels: {
    readonly XSMALL: {
        readonly provider: "openai-api";
        readonly modelId: "llama3.2:3b";
        readonly endpoint: "http://localhost:11434/v1";
        readonly apiKey: "ollama";
    };
    readonly SMALL: {
        readonly provider: "openai-api";
        readonly modelId: "llama3.1:8b";
        readonly endpoint: "http://localhost:11434/v1";
        readonly apiKey: "ollama";
    };
    readonly MEDIUM: {
        readonly provider: "openai-api";
        readonly modelId: "deepseek-r1:14b";
        readonly endpoint: "http://localhost:11434/v1";
        readonly apiKey: "ollama";
    };
    readonly LARGE: {
        readonly provider: "openai-api";
        readonly modelId: "llama3.3:70b";
        readonly endpoint: "http://localhost:11434/v1";
        readonly apiKey: "ollama";
    };
    readonly XLARGE: {
        readonly provider: "openai-api";
        readonly modelId: "llama4:16x17b";
        readonly endpoint: "http://localhost:11434/v1";
        readonly apiKey: "ollama";
    };
};

// @public (undocumented)
export interface StreamOptions<TContext = DefaultToolContext> {
    channelId?: string;
    context?: TContext;
    // (undocumented)
    conversationId?: string;
    userId?: string;
}

// @public (undocumented)
export interface TokenUsage {
    // (undocumented)
    inputTokens: number;
    // (undocumented)
    outputTokens: number;
    // (undocumented)
    totalTokens: number;
}

// @public (undocumented)
export interface ToolCallRecord {
    // (undocumented)
    error?: string;
    // (undocumented)
    input: JSONValue;
    // (undocumented)
    output: JSONValue;
    // (undocumented)
    toolName: string;
    // (undocumented)
    toolUseId: string;
}

// @public (undocumented)
export interface ToolDefinition<TContext = DefaultToolContext, TParams extends z.ZodType = z.ZodType<any>> {
    // (undocumented)
    description: string;
    handler: (args: ToolHandlerArgs<z.infer<TParams>, TContext>) => Promise<JSONValue>;
    interrupt?: (args: ToolHandlerArgs<z.infer<TParams>, TContext>) => void;
    name?: string;
    needsApproval?: boolean;
    // (undocumented)
    parameters: TParams;
    trustable?: boolean;
}

// @public
export type ToolFactory<TContext = DefaultToolContext> = <TParams extends z.ZodType>(tool: ToolDefinition<TContext, TParams>) => AgentTool<TContext>;

// @public
export interface ToolHandlerArgs<TInput = any, TContext = DefaultToolContext> {
    context: TContext;
    input: TInput;
    interrupt: <T = JSONValue>(params: {
        name: string;
        reason?: any;
    }) => T;
}

// @public
export type ToolsConfig<TContext = DefaultToolContext> = (tool: ToolFactory<TContext>) => Record<string, AgentTool<TContext>>;

// (No @packageDocumentation comment for this package)

```
